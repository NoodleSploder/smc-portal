import { Box, Heading, Text } from '@chakra-ui/react';

const Jupyter = () => {
  return (
    <Box p={8}>
      <Heading>Jupyter Page</Heading>
      <Text mt={4}>This is a new page.</Text>
    </Box>
  );
};

export default Jupyter;
